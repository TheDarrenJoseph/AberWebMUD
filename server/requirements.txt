flask >= 1.1.1
flask-socketio >= 4.1.0

jsonpickle >= 0.9.4
passlib >= 1.7.1
psycopg2-binary >= 2.7
argon2_cffi >= 16.3.0
pony >= 0.7.1
#Server libraries
eventlet >= 0.21.0

#server package  -- May not fully support WebSockets
#gevent >= 1.2.1
#Alternative server package
#uwsgi
