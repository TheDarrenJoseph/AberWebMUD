{
	"private": true,
	"scripts": {
		"build-src": "npx webpack --config webpack.config.js",
		"build-test": "npx webpack --config webpack.test.js",
		"build": "npm run build-src && npm run build-test",
		"test": "npm run build-test && npx karma start"
	},
	"dependencies": {
		"webpack": "^4.25.1"
	},
	"devDependencies": {
		"source-map-loader": ">=0.2.4",
		"karma": ">=3.1.1",
		"karma-firefox-launcher": ">=1.1.0",
		"karma-qunit": ">=2.1.0",
		"karma-sourcemap-loader": ">=0.3",
		"qunit": "^2.8.0"
	}
}
